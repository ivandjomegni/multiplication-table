This is a PHP script using nested for loops to create a multiplication table. 

The script provides a way to ask the user for the limits and then display the results in a nicely formatted HTML table.
